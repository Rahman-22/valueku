namespace ValueKu.Common;

public static class AuthConstants
{
    /// <summary>Temporary cookie that holds the external (Google) identity during sign-in.</summary>
    public const string ExternalScheme = "External";
}
