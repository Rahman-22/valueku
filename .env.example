# Copy to .env (which is gitignored) and set a strong password.
# This must match the password in your local ConnectionStrings:DefaultConnection user-secret.
# SQL Server requires 8+ chars with upper, lower, digit and symbol.
MSSQL_SA_PASSWORD=Your_Strong_Password#123
